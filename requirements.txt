# ── 运行核心（默认 akshare / qcore 免 token 数据源都需要）────────────────
akshare>=1.14.0          # 默认数据源：免 token 现拉日线（新浪源，前复权）
pandas>=2.0.0            # 数据处理
pyarrow>=14.0.0          # 读取 qcore 本地 Parquet 数据湖
python-dotenv>=1.0.0     # .env 配置加载
requests>=2.28.0         # HTTP

# ── 可选：Tushare 后端（DATA_MODE=jnb，已休眠保留，需 Token + 中转 URL）──
#    见 docs/adr/0001-retire-tushare-backend.md
#    pip install "tushare>=1.4.0"
#
# ── 可选：语料采集工具（下载/转写 B 站音频，跑行情用不到）─────────────────
#    pip install "yt-dlp>=2024.1.0" "faster-whisper>=1.0.0"
